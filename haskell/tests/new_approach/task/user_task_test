import Test.Hspec
import UserCode (Account(..)) 

instance Account Int where
    getPlayedTime a = a

main = hspec $ do
    describe "have a good day" $ do
        it "really that is" $ do
            getPlayedTime (10 :: Int) `shouldBe` 10
